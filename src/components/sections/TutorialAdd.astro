---
// Tutorial: How to Add Glass
---

<section class="scroll-section gradient-radial-purple" data-section="tutorial-add">
  <div class="tutorial-container">
    <div class="tutorial-content glass-panel">
      <span class="tutorial-tag glass">Tutorial</span>
      <h2 class="section-title">How to Add Glass Effect</h2>
      
      <div class="steps">
        <div class="step glass-card">
          <span class="step-number">1</span>
          <div class="step-content">
            <h3>Set Background</h3>
            <code>background: rgba(255, 255, 255, 0.1);</code>
            <p>Use semi-transparent white or black</p>
          </div>
        </div>
        
        <div class="step glass-card">
          <span class="step-number">2</span>
          <div class="step-content">
            <h3>Add Backdrop Filter</h3>
            <code>backdrop-filter: blur(10px);</code>
            <p>This creates the frosted glass effect</p>
          </div>
        </div>
        
        <div class="step glass-card">
          <span class="step-number">3</span>
          <div class="step-content">
            <h3>Add Subtle Border</h3>
            <code>border: 1px solid rgba(255, 255, 255, 0.18);</code>
            <p>Defines edges with subtle highlight</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .scroll-section {
    min-width: 100vw;
    height: 100vh;
    scroll-snap-align: start;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
  }
  
  .tutorial-container {
    max-width: 800px;
    width: 100%;
  }
  
  .tutorial-content {
    padding: 3rem;
  }
  
  .tutorial-tag {
    display: inline-block;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.875rem;
    font-weight: 600;
    margin-bottom: 1rem;
  }
  
  .section-title {
    font-size: 2.5rem;
    font-weight: 600;
    margin: 0 0 2rem 0;
  }
  
  .steps {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  
  .step {
    padding: 1.5rem;
    border-radius: 12px;
    display: flex;
    gap: 1.5rem;
  }
  
  .step-number {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 600;
  }
  
  .step-content h3 {
    margin: 0 0 0.75rem 0;
    font-size: 1.25rem;
  }
  
  code {
    display: block;
    background: rgba(0, 0, 0, 0.3);
    padding: 0.75rem 1rem;
    border-radius: 8px;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    margin: 0.5rem 0;
    overflow-x: auto;
  }
  
  .step-content p {
    margin: 0.5rem 0 0 0;
    opacity: 0.8;
    font-size: 0.938rem;
  }
  
  @media (max-width: 768px) {
    .tutorial-content {
      padding: 2rem;
    }
    
    .step {
      flex-direction: column;
      gap: 1rem;
    }
  }
</style>

<script>
  import { gsap, ScrollTrigger } from '../../utils/gsap-setup';
  
  document.addEventListener('DOMContentLoaded', () => {
    const section = document.querySelector('[data-section="tutorial-add"]');
    if (!section) return;
    
    gsap.from('[data-section="tutorial-add"] h2', {
      scrollTrigger: {
        trigger: section,
        start: 'left 80%',
        horizontal: true,
        scroller: '.horizontal-scroll-container',
        once: true
      },
      opacity: 0,
      x: -50,
      duration: 0.7,
      ease: 'power2.out'
    });
    
    gsap.from('[data-section="tutorial-add"] .step, [data-section="tutorial-add"] .tutorial-step', {
      scrollTrigger: {
        trigger: section,
        start: 'left 75%',
        horizontal: true,
        scroller: '.horizontal-scroll-container',
        once: true
      },
      opacity: 0,
      y: 40,
      duration: 0.6,
      stagger: 0.15,
      ease: 'back.out(1.2)',
      delay: 0.2
    });
  });
</script>
